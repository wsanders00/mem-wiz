# mem-wiz

This repository is split between a minimal project root and the shipped Python
source tree under `src/`.

- The repository root holds contributor-facing files such as `README.md`,
  `pyproject.toml`, and shared test configuration.
- `src/` is the runtime skill root for the `memwiz` package and CLI entrypoint.
